export enum Primary {
  EXECUTE = 'execute',
  ASK = 'ask',
  VERSION = 'version',
  HELP = 'help',
  FILE = 'file',
  CONFIG = 'config',
  PIPED = 'piped',
}

export enum ConfigSubCommand {
  GET = 'get',
  SET = 'set',
}
