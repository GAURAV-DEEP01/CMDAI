export enum Primary {
  EXECUTE = 'execute',
  ASK = 'ask',
  VERSION = 'version',
  HELP = 'help',
  FILE = 'file',
  CONFIG = 'config',
}

export enum ConfigSubCommand {
  GET = 'get',
  SET = 'set',
}
